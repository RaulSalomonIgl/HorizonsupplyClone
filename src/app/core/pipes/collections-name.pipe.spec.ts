import { CollectionsNamePipe } from './collections-name.pipe';

describe('CollectionsNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CollectionsNamePipe();
    expect(pipe).toBeTruthy();
  });
});
